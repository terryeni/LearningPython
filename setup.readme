Creating virtual enviroments in vscode

https://code.visualstudio.com/docs/python/environments

# macOS/Linux
# You may need to run sudo apt-get install python3-venv first
python3 -m venv .venv

# Windows
# You can also use py -3 -m venv .venv
python -m venv .venv